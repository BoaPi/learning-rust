use playground::*;

// individual lesson are written in the lin.rs file
fn main() {
    custom_enum_lesson();
    immutability_lesson();
    shadowing_lesson();
    simple_data_type();
    basic_numeric_operations();
}
